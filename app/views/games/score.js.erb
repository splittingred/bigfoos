<% if @player.game.finished? %>
window.location.href = "<%= games_path(@player.team.game) %>";
<% else %>
$('#player-score-<%= @player.id.to_s %>').text(": <%= @player.points.to_s %>");
$('#team-score-<%= @player.team.id.to_s %>').text("<%= @player.team.score.to_s %>");
<% end %>